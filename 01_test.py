# Basic OpenCV verification script.
# This file checks that the OpenCV library is installed correctly.
# It prints the OpenCV version and confirms that the import works.

import cv2

print("OpenCV version:", cv2.__version__)
print("OpenCV is working!")